package viewmodel;

import model.MovieRecommenderModel;

/**
 * This class represents the ViewModel of the application and handles
 * interaction the View and the Model.
 */
public class MovieRecommenderViewModel implements IViewModel<MovieRecommenderModel> {
    @Override
    public void connectTo(MovieRecommenderModel model) {
    }
}
