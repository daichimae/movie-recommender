package view;

import viewmodel.MovieRecommenderViewModel;

/**
 * This class represents the View of the application. It provides a UI and
 * sends events to the ViewModel.
 */
public class MovieRecommenderView implements IView<MovieRecommenderViewModel> {
    @Override
    public void bind(MovieRecommenderViewModel viewModel) {
    }
}
